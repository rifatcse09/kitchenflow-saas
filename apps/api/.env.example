# Local API port (HTTP base: http://localhost:4000)
# All routes are prefixed with /api/v1 (e.g. POST /api/v1/auth/login)
PORT=4000

# Local Postgres from docker-compose.yml
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/restaurant_platform
